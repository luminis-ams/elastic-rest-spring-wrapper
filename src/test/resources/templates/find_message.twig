{
    "query":{
{% if (length(message)==0) %}
        "match_all": {}
{% else %}
        "match":{
            "message": {
                "query": "{{ message }}",
                "operator": "{{ default(operator, 'or') }}"
            }
        }
{% endif %}
    }
}
